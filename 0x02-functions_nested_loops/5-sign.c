#include "holberton.h"

/*
 * print_sign - prints sign
 * Return: 1 if > 0, 0 if = 0, -1 if < 0
 */

int print_sign(int n)
char x
{

if (n > 0)
{
return (1);
_putchar('+');
}

else if (n == 0)
{
return (0);
_putchar('0');
}

else
{
return (-1);
_putchar('-');
}

}
